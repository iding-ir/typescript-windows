import { Bounds, Size, Sizes } from "../components/Window";
export declare const useLimits: (headerHeight: number, windowSizes: Sizes, gridsHeight: number, gridsWidth: number, gridsGap: number, bounds: Bounds | undefined, size: Size, id: string) => {
    limits: {
        left: number;
        top: number;
        right: number;
        bottom: number;
    } | undefined;
};
//# sourceMappingURL=useLimits.d.ts.map